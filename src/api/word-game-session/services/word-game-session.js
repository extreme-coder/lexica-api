'use strict';

/**
 * word-game-session service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::word-game-session.word-game-session');
